// This file was an early integration into the legacy /sensors page that no
// longer exists in main. The activities UI now lives inside DeviceDrawer.tsx.
// Safe to delete; kept as a stub so any stale imports fail loudly.

export {};
