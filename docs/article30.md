# Records of Processing Activities (GDPR Article 30)

**Last updated:** 2026-05-08
**Document version:** v1
**Maintained in lockstep with code in this repository.**

## 1. Controller

| Field | Value |
|---|---|
| Legal name | Sjølyst Innovations |
| Trading name | Garge |
| Org. number | 934 531 035 |
| Address | Mårvegen 21a, 4347 Lye, Norway |
| General contact | sondresjoelyst@gmail.com |
| Privacy contact | privacy@garge.no |

No joint controllers. No DPO designated (not required — no special-category processing, no large-scale public-area monitoring, fewer than 250 employees).

## 2. Categories of data subjects

- **Customers** — natural persons who register an account to use Garge.
- **Service-account operators** — internal staff with admin/operator roles. Covered by employment agreement; no separate consent flow.

## 3. Processing activities

### 3.1 Account management

| Field | Value |
|---|---|
| Purpose | Provide authenticated access to the service |
| Lawful basis | Art. 6(1)(b) Contract |
| Categories of data | First name, last name, email, phone, password hash, terms-acceptance record (timestamp + version + truncated IP), soft-delete flag |
| Source | Customer at registration |
| Recipients | Self-hosted PostgreSQL only |
| Retention | Until customer soft-deletes their account; PII scrubbed at deletion, row retained for FK integrity |
| Security | BCrypt password hash; JWT (HS256) bearer tokens with 1h access + 6mo refresh; refresh tokens hashed at rest; SHA-256 verification codes; account lockout after 5 failed attempts (30 min) |
| Cross-border transfer | None |

### 3.2 Subscription + billing

| Field | Value |
|---|---|
| Purpose | Process recurring subscription payments and produce legally-required invoices |
| Lawful basis | Art. 6(1)(b) Contract; Norwegian bokføringsloven §13 for invoice retention |
| Categories of data | Vipps agreement ID, charge IDs, billing address, consent timestamp, truncated consent IP (/24 IPv4 / /48 IPv6) |
| Source | Customer via Vipps consent flow |
| Recipients | Vipps Mobilepay AS (Norway, EEA) |
| Retention | Subscription lifetime + 5 years from last invoice |
| Security | Webhook HMAC-SHA256 with replay window; Idempotency-Key on all Vipps API calls; webhook secrets encrypted via ASP.NET DataProtection |
| Cross-border transfer | None |

### 3.3 Sensor + switch monitoring

| Field | Value |
|---|---|
| Purpose | Operate the service: display readings, run automation rules, send alerts |
| Lawful basis | Art. 6(1)(b) Contract |
| Categories of data | Sensor readings, switch state changes, timestamps, user-defined custom names |
| Source | Customer's own MQTT-connected devices |
| Recipients | Self-hosted PostgreSQL + EMQX broker |
| Retention | Lifetime of subscription. After account soft-delete, the link to the customer (UserSensors / UserSwitches) is severed, leaving readings effectively anonymized |
| Security | EMQX per-device PBKDF2-SHA512 credentials, per-topic ACLs, RBAC at API layer |
| Cross-border transfer | None |

### 3.4 Transactional email

| Field | Value |
|---|---|
| Purpose | Send account confirmation, password reset, billing/subscription notifications, invoices |
| Lawful basis | Art. 6(1)(b) Contract |
| Categories of data | Email address, first name, invoice content (name, address, line items) |
| Source | Generated by service in response to customer actions |
| Recipients | Brevo (Sendinblue SAS, France, EEA) |
| Retention | Per Brevo's processor retention policy; controller does not store delivery records beyond what is in DB |
| Security | TLS in transit; Brevo API key in env-only; verification codes hashed before storage |
| Cross-border transfer | None |

### 3.5 Push notifications

| Field | Value |
|---|---|
| Purpose | Deliver alerts to customer's browser/device |
| Lawful basis | Art. 6(1)(a) Consent (browser permission prompt) |
| Categories of data | Web Push endpoint URL, P256DH key, auth secret |
| Source | Browser via Web Push API after user grants permission |
| Recipients | Browser vendor's push service (Google FCM, Apple APNs, Mozilla) |
| Retention | Until customer revokes browser permission OR soft-deletes account |
| Security | Stored server-side; sent over HTTPS to vendor endpoint with VAPID-signed JWT |
| Cross-border transfer | Push to vendor endpoints may transit US infrastructure. Limited to push payload (notification title/body) — no other PII |

### 3.6 Logging and metrics

| Field | Value |
|---|---|
| Purpose | Diagnostics, security monitoring, fraud prevention |
| Lawful basis | Art. 6(1)(f) Legitimate interest |
| Categories of data | Opaque user ID (GUID), HTTP method/path, timestamps. Authorization, Cookie, Set-Cookie, X-Api-Key, Idempotency-Key, Proxy-Authorization headers redacted. No request bodies. No email/name |
| Source | Auto-generated by API at request time |
| Recipients | Self-hosted Loki (logs), Prometheus (metrics) |
| Retention | Loki: 90 days. Prometheus: 60 days |
| Security | Cluster-internal access only; no public ingress on log/metric endpoints |
| Cross-border transfer | None |

### 3.7 Database backups

| Field | Value |
|---|---|
| Purpose | Disaster recovery |
| Lawful basis | Art. 6(1)(f) Legitimate interest |
| Categories of data | Snapshot of all production data |
| Source | Automated snapshot of PostgreSQL volume |
| Recipients | Self-hosted snapshot store |
| Retention | 3 daily, 4 weekly, 3 monthly, 1 yearly |
| Note on erasure | After a customer soft-deletes, residual scrubbed data may persist in backups for up to 12 months until the yearly rotation completes. Backups are encrypted at rest, are not used for any processing, and are restored only on disaster |
| Security | Encrypted at rest |
| Cross-border transfer | None |

## 4. Sub-processors

| Processor | Country | Activity | DPA in place |
|---|---|---|---|
| Vipps Mobilepay AS | Norway (EEA) | Payments, userinfo for billing address | Yes |
| Sendinblue SAS (Brevo) | France (EEA) | Transactional email delivery | Yes |
| Google / Apple / Mozilla push services | Various | Web Push delivery (no DPA possible — browser-mediated; covered by user consent) | N/A |

The Garge platform is **self-hosted** by Sjølyst Innovations on infrastructure under its own direct control. There is no upstream cloud or hosting provider in the chain.

## 5. Data subject rights — implementations

- **Access (Art. 15) + Portability (Art. 20):** `GET /api/users/{id}/export` returns JSON with all customer data (account, sensors, switches, readings, activities, photos).
- **Rectification (Art. 16):** `PUT /api/users/{id}/profile` (name + phone) and `PUT /api/users/{id}/preferences` (settings). Email change requires re-verification (planned follow-up).
- **Erasure (Art. 17):** `DELETE /api/users/{id}/account` — soft-deletes the user (PII scrubbed, account locked), retains Orders and Invoices for the 5-year bokføringsloven obligation, deletes RefreshTokens / PushSubscriptions / WebhookSubscriptions / custom names / activities.
- **Restriction / Object (Art. 18, 21):** No automated decision-making. No direct-marketing processing. Users can soft-delete to stop processing.
- **Withdraw consent (Art. 7(3)):** Push permissions: revoke in browser settings. Subscription waiver of right of withdrawal: cannot be retroactively withdrawn (Angrerettsloven §22, by design).

## 6. Breach response

- 72-hour Datatilsynet notification window per Art. 33.
- Customer notification per Art. 34 if high risk.
- Internal incident playbook tracked separately.

## 7. Document maintenance

- Update this file in the same PR as any change to data processing (new processor, new collected field, changed retention, etc.).
- Reviewed at least annually.
